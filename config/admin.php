<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Admin Email
    |--------------------------------------------------------------------------
    |
    | The user with this email address can open the admin panel at /admin,
    | where every user and organization can be managed. Nobody else can, and
    | when it is left empty the panel is closed to everyone.
    |
    */

    'email' => env('ADMIN_EMAIL'),

];
